# Production Mode

Elf runs some additional checks to improve the user experience in development. You should disable it in production by calling `enableElfProdMode`:

```ts
import { enableElfProdMode } from '@ngneat/elf';

if (production) {
  enableElfProdMode();
}
```
